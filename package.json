{
  "name": "time-blocked-todo",
  "version": "0.1.0",
  "private": true,
  "description": "Time-blocked todo web app for Cloudflare Pages with KV-backed cross-device sync.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
